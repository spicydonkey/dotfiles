- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: 
    ~/.bashrc: 
    ~/.gitconfig: 

- create:
    - ~/laboro

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
