- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: 
    ~/.bashrc: 
    ~/.bashrc_global:
    ~/.bashrc_non_interactive:
    ~/.gitconfig: 
    ~/.inputrc:

- create:
    - ~/laboro

- shell:
    - 
      command: ./setup_apt.sh
      stdout: true
      stderr: true
