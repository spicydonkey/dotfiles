- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc

- create:
    - ~/laboro

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
