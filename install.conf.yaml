- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: 
    ~/.vimrc: 
    ~/.bashrc: 
    ~/.gitconfig: 

- create:
    - ~/laboro

- shell:
    - 
      command: ./setup_apt.sh
      stdout: true
      stderr: true
